{
  "name": "weekflow-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "native": "npm run start -w @weekflow/native",
    "web": "npm run dev -w @weekflow/web",
    "test": "npm run test -w @weekflow/native"
  }
}
